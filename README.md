# supungbab/homebrew-tap

```
brew install --cask supungbab/tap/offiky
```

업데이트:

```
brew update && brew upgrade --cask offiky
```

`brew update` 를 빼면 tap 을 다시 읽지 않아 새 버전을 모른다.

[offiky](https://github.com/supungbab/offiky) — 같은 네트워크 동료들의 픽셀 캐릭터가
화면 바닥을 돌아다니는 맥 메뉴바 앱.
